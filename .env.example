# Production Environment Variables
# Copy this to .env and update values
# NEVER commit .env file to git!

# ============================================
# DATABASE CONFIGURATION
# ============================================
MYSQL_ROOT_PASSWORD=generate_strong_password_min_20_chars
MYSQL_USER=lms_prod_user
MYSQL_PASSWORD=generate_strong_password_min_20_chars

# ============================================
# JWT SECURITY (CRITICAL!)
# ============================================
# Generate a secure random string with at least 64 characters
# You can use: openssl rand -base64 64
JWT_SECRET=REPLACE_THIS_WITH_64_CHAR_RANDOM_STRING_USE_OPENSSL_RAND_BASE64_64

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins (your frontend URLs)
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================
# API URL (for frontend build)
# ============================================
API_URL=https://your-domain.com/api
